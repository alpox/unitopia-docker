-------------------------------------------------------------------------------
zsp[iele] [-a] [<Objektstring>]
        Zeigt die Spiele an, die der Spieler <Objektstring> gespielt hat.
        Wenn -a angegeben wurde zeigt es alle Spiele an, d.h. auch die
        nicht gespielten.
Siehe auch: zraetsel
-------------------------------------------------------------------------------
